import { tv } from 'tailwind-variants'

export const slots = tv({
  slots: {
    heroContainer: 'relative z-20 container grid grid-cols-12 px-4',
    heroContent: 'col-span-10 col-start-2 text-center'
  }
})
