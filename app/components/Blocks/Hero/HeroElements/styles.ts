import { tv } from 'tailwind-variants'

export const slots = tv({
  slots: {
    heroTitle: 'leading-tight font-semibold',
    heroDescription: 'text-hero-text leading-normal mt-6',
    heroImage: 'ease-in-out transition-all duration-500'
  }
})
