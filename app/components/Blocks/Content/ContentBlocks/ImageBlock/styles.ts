import { tv } from 'tailwind-variants'

export default tv({
  slots: {
    imageContainer: 'text-center',
    imageElement: 'block m-auto max-w-full',
    imageCaption: 'block bg-light-hover/20 italic px-4 py-2 text-center'
  }
})
