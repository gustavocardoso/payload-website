import { tv } from 'tailwind-variants'

export default tv({
  slots: {
    videoContainer: 'video-container aspect-video w-full m-auto',
    videoCaption: 'block bg-light-hover/20 italic px-4 py-2 text-center'
  }
})
